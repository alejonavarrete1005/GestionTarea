@model IEnumerable<Proyecto>

@{
    ViewData["Title"] = "Proyectos";
}

<h2>Proyectos</h2>

<a asp-action="Crear" class="btn btn-primary mb-3">Nuevo Proyecto</a>

<table class="table">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Fecha Inicio</th>
            <th>Fecha Fin</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var proyecto in Model)
        {
            <tr>
                <td>@proyecto.Nombre</td>
                <td>@proyecto.FechaInicio.ToShortDateString()</td>
                <td>@proyecto.FechaFin.ToShortDateString()</td>
                <td>
                    <a asp-action="Editar" asp-route-id="@proyecto.Id" class="btn btn-sm btn-warning">Editar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
